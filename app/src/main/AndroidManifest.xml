<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taskcademia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Taskcademia">
        <activity android:name=".View.DueDate" />
        <activity android:name=".View.SignUpView" />
        <activity android:name=".View.LogoutActivity" />
        <activity android:name=".View.LoginView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.ProfileAct" />
        <activity android:name=".View.FoodAct" />
        <activity android:name=".View.TeachersAct" />
        <activity android:name=".View.CourseAct" />
        <activity android:name=".View.home" />
    </application>

</manifest>